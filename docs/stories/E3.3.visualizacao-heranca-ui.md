---
epic: E3
epicTitle: "🧩 Templates de Qualidade com Herança Hierárquica"
story: E3.3
status: Done
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [code_review, ui_consistency, ux_review]
prd_ref: "docs/prd/top-of-mind-analytics-prd.md — Seção 9.2 (Qualidade)"
depends_on: "E3.2"
---

# Story E3.3: Visualização de Herança na Aba 🔬 Pesquisa

## Status
Done

## Story
**As a** administrador configurando a pesquisa,
**I want** ver a representação visual da árvore hierárquica das minhas categorias revelando de onde vem as obrigações qualitativas atreladas,
**so that** não fique cego sobre se certas subdivisões (como Pizzaria) ficaram presas em genéricos inadequados (ex. Gastronomia Geral), e eu possa consertar overrides rápidos na mesma tela.

## Acceptance Criteria

1. - [x] Interface da Lista Visual carregada em `ResearchTab.tsx` sob a sessão de "Qualidade" (Toggled via "Incluir perguntas de qualidade").
2. - [x] A renderização deve ilustrar graficamente (Lista ou TreeNode/TreeGraph vertical baseado na estrutura de indentação CSS). Ex. Pai forte, filhos indentados.
3. - [x] Os dados devem vir de uma query única por debaixo dos panos que traga as categorias da enquete corrente parseando o `resolverTemplate` do E3.2 batch, não travando UI (component async).
4. - [x] Indicadores visuais: 
      - ✅ (Ícone check verde) Templates diretos.
      - ⚡ (Ícone Sparkle azul) para Overrides ocorrendo nos filhos (desviou da família principal).
      - ⚠️ (Aviso âmbar) Categorias sem nenhum template engatado nativo/pai.
5. - [x] Componente Popover/Select In-line nos botões gerentes permitindo ali o link de um template para o pai ou override isolado no filho (Dropdown exibindo lista do E3.1).
6. - [x] Integração com Segmento Backend: A ação de vincular template atualiza via PUT `api/segmentos/[id]` mandando `templateQualidadeId` para sanar a árvore.
7. - [x] Tooltips via shadcn informando `Herdado de: X` passando o mouse por cima do check de herdeiro.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled

### Story Type Analysis
**Primary Type**: Frontend UI (Tree Visualization)
**Complexity**: Medium-High (Nested mapping, Data Fetching Batch)

### Specialized Agent Assignment
**Primary Agents**: @dev

### Quality Gate Tasks
- [x] Pre-Commit (@dev): Desempenho do parser de arrays evitando Loops infindos na UX.

## Tasks / Subtasks

- [x] Task 1: Endpoint Auxiliar de Agregação da Qualidade (AC: 3)
  - [x] Rota `GET /api/enquetes/admin/[id]/arvore-qualidade` retornando um payload estruturado ou extender o `/api/segmentos?tree=true` incluindo resolução para cada item listado de forma ágil que satisfaça a hierarquia (Dica: Busque todos e cruze em memo na RAM antes de emitir o JSON para ser perfomático).

- [x] Task 2: UI Visualização de Árvore (AC: 1, 2, 4, 7)
  - [x] Criar `/src/components/admin/enquete/QualidadeInheritanceView.tsx`.
  - [x] Implementar a arvore (Componente hierárquico). Usar identação padrão (ex: `pl-4 border-l-2`).
  - [x] Incluir lógicas condicionais atreladas por badges: `<Badge variant='outline'>✅ Herdado</Badge>`, alertas etc.
  - [x] Engatar o componente ao fim de `ResearchTab` (apenas quando `watch('incluirQualidade')` for true).

- [x] Task 3: Mini-Editor na Árvore (AC: 5, 6)
  - [x] No fim do nó gerado na UI, adicionar botão icone Edit/Gear ou Combo-box (`Select`). 
  - [x] Fetch assíncrono básico listando todos Templates da Org num state array.
  - [x] OnChange deste `select`, realizar POST/PUT patch para a rota associada daquele Segmento conectando-o no `templateQualidadeId`. Revalidar o Request do componente Arvore no sucesso (TanStack refetch ou setter local otimista).

## Dev Notes

Pense que a UI desta feature deve ajudar o operador do Prêmio, que é ocupado, a auditar sem precisar ir página por página na área de Segmentos do admin comum. Centralizar a configuração do Override (E3) na Aba de Pesquisa acelera muito o "Onboarding" gerencial da eleição.

Assegure que os dados que alimentem `QualidadeInheritanceView.tsx` restrinjam a "Árvore" SOMENTE aos segmentos já habilitados NAQUELA TELA DE ENQUETE (via EnqueteSegmentos no backend base, mas também reativo aos segmentos guardados via FrontEnd Local caso o usuário não tenha salvado a rascunho. Para escopo inicial pode atrelar unicamente aos atrelados já pós-save por simplicidade backend).

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-21 | 1.0 | Aba pesquisa e tree logic (E3.3) | River (SM) |

## Dev Agent Record

### Agent Model Used
Gemini Exp 1206 (via cursor/synkra)

### Completion Notes List
- Criei o componente `QualidadeInheritanceView` para buscar e renderizar a árvore de qualidade com base na enquete id. Ele constrói dinamicamente a hierarquia em memória de segmento pai e filhos e mostra badges correspondentes (Herdado, Local Override, Sem Template).
- Para simplificar e evitar dependências complexas (falha de import no Shadcn Select), usei um `select` HTML nativo customizado com classes do TailwindCSS, o que proporciona uma experiência de uso excelente nativa do navegador para listas de `<option>`.
- Fui ao backend em `segmentos/[id]/route.ts` e adicionei validação e suporte de `templateQualidadeId` além do `paiId`, permitindo a atualização via PUT por um administrador.
- Acoplei o componente renderizado ao fundo da tab `ResearchTab` sob o toggle `incluirQualidade`.

### File List
- `src/components/admin/enquete/QualidadeInheritanceView.tsx` (Novo)
- `src/components/admin/enquete/tabs/ResearchTab.tsx` (Editado)
- `src/app/api/segmentos/[id]/route.ts` (Editado)
- `src/lib/segmentos/segmento-service.ts` (Editado)


### QA Agent Record
- Typecheck, testes unitários testados e lint (resolvido).
- Critérios de aceite validados e confirmados na base.
- Mudado status de revisão para finalizado (Done).
